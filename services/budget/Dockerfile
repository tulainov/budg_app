FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY *.go schema.sql ./
RUN CGO_ENABLED=0 GOOS=linux go build -o /out/budget .

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /out/budget /budget
USER nonroot:nonroot
EXPOSE 8080
ENTRYPOINT ["/budget"]
